#pragma once

#include <fstream>
#include <iostream>
#include <string>
#include <vector>

extern void read_file(std::vector<std::string> &Ma, std::vector<std::string> &MO);
extern void colorInput(std::string word, char green[5], char orange[5], std::vector<char> &gray);
